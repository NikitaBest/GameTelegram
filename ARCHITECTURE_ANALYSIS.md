# Анализ архитектуры проекта GameTelegram

## 📋 Обзор проекта

**GameTelegram** - это Telegram Web App для проведения игровых розыгрышей. Пользователи подписываются на партнерские каналы, играют в мини-игры и соревнуются за призы.

### Технологический стек
- **React 19.2** - UI библиотека
- **TypeScript** - Типизация (частично, микс с JavaScript)
- **Vite 7.2** - Сборщик и dev-сервер
- **Tailwind CSS** - Утилитарная стилизация
- **Telegram Web App SDK** - Интеграция с Telegram
- **Framer Motion** - Анимации (опционально)
- **React Router DOM** - Навигация

---

## 🏗️ Архитектура приложения

### Структура проекта

```
src/
├── api/                    # API клиент и сервисы
│   ├── apiClient.ts       # Базовый HTTP клиент
│   └── services/          # API сервисы (auth, draw, attempt, leaderboard)
├── components/            # Переиспользуемые UI компоненты
├── pages/                 # Страницы приложения
│   ├── ActiveDrawsPage   # Список активных розыгрышей
│   ├── DrawPage          # Страница розыгрыша (главная)
│   ├── GameResultsPage   # Страница результатов игры
│   └── PathToTreasuresPage # Страница подписок на партнеров
├── game/                  # Игровая логика
│   ├── GameRouter.tsx    # Роутер для выбора игры
│   ├── flappy-bird/      # Игра Flappy Bird (gameId: 2)
│   └── game cosmos/      # Космическая игра (gameId: 1)
├── hooks/                 # React хуки
│   ├── useAuth.ts        # Авторизация
│   └── useGameLogic.ts   # Логика космической игры
├── lib/                   # Библиотеки и типы
│   ├── game-types.ts     # Типы для игр
│   └── telegram.ts       # Утилиты Telegram Web App
└── utils/                 # Утилиты
```

### Поток данных приложения

```
1. Пользователь открывает приложение
   ↓
2. Авторизация через Telegram Web App (useAuth)
   ↓
3. Парсинг параметра tgWebAppStartParam (drawId)
   ↓
4. Проверка подписок на партнеров (checkPartnersSubscription)
   ↓
5. Загрузка данных розыгрыша (startDraw) → получение gameId
   ↓
6. DrawPage отображает информацию о розыгрыше
   ↓
7. Пользователь нажимает "Начать игру"
   ↓
8. GameRouter выбирает игру по gameId
   ↓
9. Игра запускается, пользователь играет
   ↓
10. Игра завершается → onGameOver(score)
    ↓
11. GameResultsPage сохраняет результат (saveAttempt)
    ↓
12. Отображение результатов и лидерборда
```

---

## 🎮 Система игр

### Текущие игры

#### 1. Flappy Bird (gameId: 2)
- **Путь**: `src/game/flappy-bird/`
- **Компонент**: `FlappyBirdGame.tsx`
- **Описание**: Классическая игра - птица пролетает между трубами
- **Управление**: Тап/Клик или пробел
- **Очки**: За каждую пройденную пару труб (+1 очко)
- **Проигрыш**: При столкновении с трубами или границами
- **Особенности**:
  - Плавная физика с гравитацией
  - Динамическая сложность (зазор уменьшается, скорость растет)
  - Экран правил перед началом

#### 2. Космическая игра (gameId: 1)
- **Путь**: `src/game/game cosmos/`
- **Компонент**: `GameContainer.tsx`
- **Описание**: Космический корабль собирает звезды и избегает астероидов
- **Управление**: Touch/Drag или клавиатура (стрелки)
- **Очки**: За собранные звезды (+10 очков)
- **Проигрыш**: При столкновении с астероидами (3 жизни)
- **Особенности**:
  - 2D движение (X и Y координаты)
  - Система жизней
  - Визуальные эффекты при сборе/столкновении
  - Использует хук `useGameLogic` для логики

### Роутинг игр

**GameRouter** (`src/game/GameRouter.tsx`) - центральный компонент для выбора игры:

```typescript
interface GameRouterProps {
  gameId: number | null;  // ID игры из бекенда
  onGameOver: (score: number) => void;  // Callback при завершении
}

// Логика выбора:
// - gameId: 1 → Космическая игра
// - gameId: 2 → Flappy Bird (по умолчанию)
// - gameId: null → Flappy Bird (fallback)
```

**Источник gameId**: Бекенд возвращает `gameId` в ответе `/participating/start`:
```typescript
// DrawPage получает gameId из ответа
const drawResponse = await startDraw(drawId);
const gameId = drawResponse.value.draw.gameId;
onGameIdReceived?.(gameId); // Передает в App.jsx
```

---

## 📐 Требования к новой игре

### Обязательный интерфейс

Все игры **ОБЯЗАНЫ** реализовывать следующий интерфейс:

```typescript
interface GameProps {
  onGameOver: (score: number) => void;
}
```

**Критически важно**: Игра должна вызвать `onGameOver(score)` когда игра заканчивается, иначе результат не будет сохранен!

### Обязательные функции

#### 1. Callback `onGameOver`
```typescript
// Пример вызова при завершении игры
const handleGameOver = () => {
  setIsGameOver(true);
  // ВАЖНО: Вызываем onGameOver с финальным счетом
  onGameOver(finalScore);
};
```

#### 2. Управление
Игра должна поддерживать:
- **Touch события** (обязательно для мобильных):
  - `onTouchStart`, `onTouchMove`, `onTouchEnd`
  - Или `onClick` для тапов
- **Клавиатуру** (опционально, для десктопа):
  - Обработка `keydown`/`keyup` событий

#### 3. Адаптивность
- Работа на мобильных устройствах (320px+)
- Поддержка планшетов
- Опционально: поддержка десктопа
- Учет safe area для iOS (`env(safe-area-inset-*)`)

#### 4. Экран правил
Перед началом игры должен отображаться экран с правилами:
- Описание управления
- Цель игры
- Как начисляются очки
- Кнопка "Начать игру" или "Тапни чтобы начать"

### Рекомендации

#### 1. Стилизация
- Используйте градиенты в стиле проекта:
  - Синие оттенки: `#407AD3`, `#487ED2`, `#80B0F6`
  - Космические градиенты для космических игр
- Шрифт: `Rubik` или `Helvetica`
- Адаптивные размеры через `clamp()`:
  ```css
  font-size: clamp(18px, 5vw, 24px);
  ```

#### 2. Производительность
- Используйте `requestAnimationFrame` для игрового цикла
- Оптимизируйте ре-рендеры через `useCallback` и `useMemo`
- Используйте `transform` вместо изменения `left/top` для анимаций
- Используйте `will-change-transform` для GPU ускорения
- Ограничивайте количество объектов на экране

#### 3. UX
- Показывайте текущий счет во время игры (HUD)
- Плавные переходы и анимации
- Визуальная обратная связь при действиях
- Предотвращение случайных закрытий (отключение свайпов)

---

## 🔧 Пошаговая инструкция добавления новой игры

### Шаг 1: Создайте папку с игрой

```bash
mkdir src/game/my-new-game
```

### Шаг 2: Создайте компонент игры

Создайте файл `MyNewGame.tsx`:

```typescript
import { useState, useEffect, useRef, useCallback } from 'react';
import './MyNewGame.css';

interface MyNewGameProps {
  onGameOver: (score: number) => void;
}

export function MyNewGame({ onGameOver }: MyNewGameProps) {
  const [score, setScore] = useState(0);
  const [isPlaying, setIsPlaying] = useState(false);
  const [isGameOver, setIsGameOver] = useState(false);
  const [showRules, setShowRules] = useState(true);
  const gameLoopRef = useRef<number | null>(null);
  const gameOverHandledRef = useRef(false);

  // Начало игры
  const startGame = useCallback(() => {
    setScore(0);
    setIsPlaying(true);
    setIsGameOver(false);
    setShowRules(false);
    gameOverHandledRef.current = false;
  }, []);

  // Игровой цикл
  useEffect(() => {
    if (!isPlaying || isGameOver) {
      if (gameLoopRef.current) {
        cancelAnimationFrame(gameLoopRef.current);
        gameLoopRef.current = null;
      }
      return;
    }

    const gameLoop = () => {
      // Ваша игровая логика здесь
      
      // Проверка условий окончания игры
      if (/* условие проигрыша */) {
        setIsGameOver(true);
        setIsPlaying(false);
        
        // ВАЖНО: Вызываем onGameOver только один раз
        if (!gameOverHandledRef.current) {
          gameOverHandledRef.current = true;
          setTimeout(() => {
            onGameOver(score);
          }, 500); // Небольшая задержка для плавности
        }
        return;
      }

      gameLoopRef.current = requestAnimationFrame(gameLoop);
    };

    gameLoopRef.current = requestAnimationFrame(gameLoop);

    return () => {
      if (gameLoopRef.current) {
        cancelAnimationFrame(gameLoopRef.current);
      }
    };
  }, [isPlaying, isGameOver, score, onGameOver]);

  return (
    <div className="my-new-game-container">
      {/* Игровая область */}
      <div className="my-new-game-area">
        {/* Фон */}
        <div className="my-new-game-background" />

        {/* Игровые объекты */}
        {!showRules && isPlaying && !isGameOver && (
          <>
            {/* Ваши игровые элементы */}
            <div className="my-new-game-hud">
              <div className="my-new-game-score">Очки: {score}</div>
            </div>
          </>
        )}

        {/* Экран правил */}
        {showRules && (
          <MyNewGameRulesScreen onStart={startGame} />
        )}
      </div>
    </div>
  );
}
```

### Шаг 3: Создайте экран правил

Создайте файл `MyNewGameRulesScreen.tsx`:

```typescript
import { Hand, Target, AlertTriangle } from 'lucide-react';

interface MyNewGameRulesScreenProps {
  onStart: () => void;
}

export function MyNewGameRulesScreen({ onStart }: MyNewGameRulesScreenProps) {
  return (
    <div 
      className="absolute inset-0 z-50 flex flex-col items-center justify-center text-white pointer-events-auto px-4"
      onClick={onStart}
      style={{
        background: 'rgba(0, 0, 0, 0.5)',
        backdropFilter: 'blur(2px)',
      }}
    >
      <div 
        className="w-full max-w-sm rounded-2xl p-5 md:p-6 mb-6"
        style={{
          background: 'rgba(173, 216, 230, 0.25)',
          backdropFilter: 'blur(12px)',
          border: '1px solid rgba(255, 255, 255, 0.3)',
          boxShadow: '0 8px 32px rgba(0, 0, 0, 0.3)',
        }}
        onClick={(e) => e.stopPropagation()}
      >
        <h2 className="text-xl md:text-2xl font-bold text-white text-center mb-5">
          Правила игры
        </h2>

        <div className="space-y-4">
          <div className="flex items-start gap-3">
            <Hand className="w-5 h-5 text-white" />
            <p className="text-white text-sm md:text-base">
              Описание управления
            </p>
          </div>
          {/* Добавьте больше правил */}
        </div>
      </div>

      <div 
        className="flex items-center gap-2.5 text-white cursor-pointer"
        onClick={onStart}
        style={{
          background: 'rgba(255, 255, 255, 0.15)',
          padding: '12px 24px',
          borderRadius: '14px',
        }}
      >
        <Hand className="w-4 h-4" />
        <span className="text-base md:text-lg font-bold">
          Тапни чтобы начать
        </span>
      </div>
    </div>
  );
}
```

### Шаг 4: Создайте стили

Создайте файл `MyNewGame.css`:

```css
.my-new-game-container {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  width: 100vw;
  height: 100vh;
  height: 100dvh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(180deg, #87CEEB 0%, #98D8E8 100%);
  touch-action: none;
  user-select: none;
  /* Safe area для iOS */
  padding-top: env(safe-area-inset-top, 0px);
  padding-bottom: env(safe-area-inset-bottom, 0px);
}

.my-new-game-area {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

/* Адаптивность для планшетов/десктопов */
@media (min-width: 481px) {
  .my-new-game-container {
    padding: 20px;
  }

  .my-new-game-area {
    max-width: 800px;
    max-height: 600px;
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }
}
```

### Шаг 5: Добавьте игру в GameRouter

Откройте `src/game/GameRouter.tsx` и добавьте:

```typescript
import { MyNewGame } from './my-new-game/MyNewGame';

export function GameRouter({ gameId, onGameOver }: GameRouterProps) {
  const selectedGameId = gameId || 2; // Flappy Bird по умолчанию

  switch (selectedGameId) {
    case 1:
      return <CosmosGame onGameOver={onGameOver} />;
    
    case 2:
      return <FlappyBirdGame onGameOver={onGameOver} />;
    
    case 3: // Новый gameId для вашей игры
      return <MyNewGame onGameOver={onGameOver} />;
    
    default:
      return <FlappyBirdGame onGameOver={onGameOver} />; // Fallback на Flappy Bird
  }
}
```

### Шаг 6: Настройте бекенд

Убедитесь, что бекенд возвращает правильный `gameId` в ответе `/participating/start`:

```json
{
  "value": {
    "draw": {
      "gameId": 3  // ID вашей новой игры
    }
  }
}
```

---

## 🔄 Интеграция с системой

### Сохранение результатов

После завершения игры результат автоматически сохраняется через `GameResultsPage`:

1. Игра вызывает `onGameOver(score)`
2. `App.jsx` переходит на страницу `results`
3. `GameResultsPage` вызывает `saveAttempt(participatingId, score)`
4. Результат сохраняется на бекенде через `/attempt/save`

**Важно**: Игра не должна самостоятельно вызывать `saveAttempt` - это делает `GameResultsPage`.

### Получение данных игры

Игра получает данные через пропсы:
- `onGameOver` - callback для завершения игры
- Все остальные данные (drawId, participatingId) доступны через контекст или пропсы родительского компонента

---

## 🎨 Стили и дизайн

### Цветовая палитра

- **Основные цвета**:
  - Синий: `#407AD3`, `#487ED2`, `#80B0F6`
  - Градиенты: `linear-gradient(180deg, #87CEEB 0%, #98D8E8 100%)`
- **Текст**: Белый с тенью для читаемости
- **Фон**: Темный с градиентами или космические темы

### Типографика

- **Шрифт**: `'Rubik', Helvetica, sans-serif`
- **Размеры**: Используйте `clamp()` для адаптивности
- **Тени**: `text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5)`

### Адаптивность

- Мобильные: 320px - 480px (полноэкранный режим)
- Планшеты: 481px+ (с отступами и скруглениями)
- Используйте `@media` запросы для разных размеров

---

## 📊 Типы и интерфейсы

### Основные типы (из `game-types.ts`)

```typescript
// Размеры игрового поля (для космической игры)
export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 600;

// Типы игровых объектов
export type GameObjectType = 'star' | 'crystal' | 'bonus' | 'asteroid' | 'enemy';

// Состояние игры
export interface GameState {
  score: number;
  multiplier: number;
  level: number;
  lives: number;
  isPlaying: boolean;
  isPaused: boolean;
  isGameOver: boolean;
  showRules: boolean;
}
```

**Примечание**: Эти типы используются космической игрой. Для новой игры вы можете создать свои типы или использовать существующие.

---

## 🧪 Тестирование

### Локальная разработка

1. Запустите dev-сервер: `npm run dev`
2. В `App.jsx` есть тестовый режим:
   ```typescript
   const [gameId, setGameId] = useState(import.meta.env.DEV ? 2 : null);
   ```
   В DEV режиме используется `gameId = 2` (Flappy Bird) для тестирования.

3. Для тестирования новой игры временно измените:
   ```typescript
   const [gameId, setGameId] = useState(import.meta.env.DEV ? 3 : null); // Ваш gameId
   ```

### Отладка

- Используйте `console.log` для отслеживания состояния
- Проверяйте вызов `onGameOver` в консоли
- Проверяйте сохранение результата в Network tab

---

## ⚠️ Важные замечания

1. **Обязательный вызов onGameOver**: Без вызова `onGameOver(score)` результат не будет сохранен
2. **Один вызов**: Используйте `useRef` для предотвращения множественных вызовов
3. **Адаптивность**: Всегда тестируйте на мобильных устройствах
4. **Производительность**: Ограничивайте количество объектов и используйте оптимизации
5. **Touch события**: Обязательно поддерживайте touch для мобильных
6. **Safe area**: Учитывайте safe area для iOS устройств

---

## 📝 Примеры простых игр для добавления

1. **Snake (Змейка)** - классическая игра
2. **Doodle Jump** - прыжки по платформам
3. **Endless Runner** - бегун с препятствиями
4. **Breakout/Arkanoid** - разбивание блоков
5. **2048** - головоломка с числами
6. **Tetris** - тетрис (более сложная)

---

## 🔗 Полезные ссылки

- [Telegram Web App Documentation](https://core.telegram.org/bots/webapps)
- [React Documentation](https://react.dev)
- [Tailwind CSS Documentation](https://tailwindcss.com)
- [TypeScript Documentation](https://www.typescriptlang.org)

---

## 📄 Заключение

Проект имеет четкую архитектуру для добавления новых игр. Основные требования:

1. ✅ Реализовать интерфейс `GameProps` с `onGameOver`
2. ✅ Вызвать `onGameOver(score)` при завершении игры
3. ✅ Добавить игру в `GameRouter`
4. ✅ Поддержать touch события для мобильных
5. ✅ Создать экран правил
6. ✅ Обеспечить адаптивность
7. ✅ Оптимизировать производительность

Следуя этим требованиям, вы сможете легко интегрировать новую игру в проект!

